import { PriorityHigh } from '@mui/icons-material';
import RIcon, { IconProps } from './icon';

export default function RCautionIcon(props: IconProps) {
  return <RIcon {...props} icon={PriorityHigh} />;
}
