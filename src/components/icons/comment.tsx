import { CommentOutlined } from '@mui/icons-material';
import RIcon, { IconProps } from './icon';

export default function RCommentIcon(props: IconProps) {
  return <RIcon {...props} icon={CommentOutlined} />;
}
