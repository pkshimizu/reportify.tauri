import { CalendarMonth } from '@mui/icons-material';
import RIcon, { IconProps } from './icon';

export default function RCalendarIcon(props: IconProps) {
  return <RIcon {...props} icon={CalendarMonth} />;
}
