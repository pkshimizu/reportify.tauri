import { ArrowForward } from '@mui/icons-material';
import RIcon, { IconProps } from './icon';

export default function RNextIcon(props: IconProps) {
  return <RIcon {...props} icon={ArrowForward} />;
}
